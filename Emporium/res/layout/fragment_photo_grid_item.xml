<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content" >

    <ImageView
        android:id="@+id/photoGridItemImageView"
        android:contentDescription="@string/dbx_listitem_imageview_desc"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_margin="10dp"
        android:layout_centerHorizontal="true"
        android:scaleType="fitCenter"
        android:background="#ff0000"
        android:src="@drawable/folder_user" />

    <TextView
        android:id="@+id/photoGridItemTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/photoGridItemImageView"
        android:layout_centerHorizontal="true"
        android:layout_marginLeft="15dp"
        android:scrollHorizontally="true"
        android:text="@string/textview_default_text"
        android:textSize="12sp"
        android:textStyle="normal" />

</RelativeLayout>
